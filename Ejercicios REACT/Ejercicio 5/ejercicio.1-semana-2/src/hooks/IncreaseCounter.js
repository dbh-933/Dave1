import { useState } from "react";

/*export default function useIncrease() {
  const [increase, setIncrease] = useState(0);

  return (
    <>
      <button id="1" onClick={() => setIncrease((prevCount) => prevCount + 1)}>
        +
      </button>
      ;
    </>
  );
}*/
