import { useState } from "react";

/*export default function useDecrease() {
  const [decrease, setDecrease] = useState(0);
  return (
    <>
      <button onClick={() => setDecrease((prevCount) => prevCount - 1)}>
        -
      </button>
    </>
  );
}*/
